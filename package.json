{
  "name": "summer-question-bingo",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time bilingual question bingo — one deployable service.",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node --env-file-if-exists=server/.env server/server.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
