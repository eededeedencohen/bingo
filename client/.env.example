# Optional. When unset the client talks to <current-hostname>:4000, which is what
# you want for LAN play (phones hitting 192.168.x.x:5173 reach 192.168.x.x:4000).
# VITE_SERVER_URL=http://localhost:4000
